# 更新日志

本文件只记**本分支相对上游 [lodebar2026/jpeditor](https://github.com/lodebar2026/jpeditor)
的改动**。上游自己的更新不在这里记。

## 2026-09-16

### 新增：谱面就地编辑与点选联动

在右边「排版」里直接改谱。点谱面上的字 → 左边源码选中并滚过去；光标停在某个词素上 →
谱面对应的字高亮；**双击**原地弹输入框改它，回车落回源码、Esc 取消。
音符 / 歌词 / 标题 / 词曲署名 / 调号拍号 / 小节线都能点，`.jpwabc` 与文本谱两种格式都有，
只在**原样档**（展开档一个对象对应多遍，指不回唯一一段原文）。

- **源码是唯一真相**：谱面上的任何编辑最终都落成「把源码某个区间换成另一段文本」，
  再由现有的实时重排把谱面重画。撤销就是 CodeMirror 自己的撤销，不另建撤销栈。
- **提交前先验，判据是「比较」不是「归零」**：改动前后各量一遍语法错误 / 诊断数，
  变差了才拦；拦下时不写进文档、输入框标红、焦点留住。
- 新增 `src/common/source.ts`（定位口径）、`src/editor/inlineedit.ts`（就地编辑）、
  `docs/实现/谱面就地编辑.md`（判据与踩坑全录）。
- 回归 `scripts/pick-check.mjs`、`scripts/edit-check.mjs`、`scripts/barline-check.mjs`。

### 修复：纸顶的调号拍号删不掉

两个毛病叠在一起：

- `painter.ts::keyMeter` 画的内容取自 `measures[0]`，与源码里写没写 `KeyAndMeters` 无关
  ——源码删干净了它照样画一个凭空的 `1=C 4/4`。现在由 `Score.showKeyMeter` 说了算：
  `.jpwabc` 那条路按「`KeyAndMeters` 有没有非空的值」定，别的来路（MusicXML / OMR）默认印。
- `TitleSection.meter` 遇到只写一半的 `KeyAndMeters`（`1=F`，或被删空）时
  `split(",")[1].trim()` 抛 TypeError，整篇 import 挂掉。现在空值当没写，缺了就走默认 `C` / `4/4`。

**代价**：从前没写 `KeyAndMeters` 的手写谱会白得一个 `1=C 4/4`，现在不印了——要印就把那一行写上。

### 修复：重排失败不再一声不响

`App.reload` 解析 / 导入 / 排版失败时留着上一版谱面（这条是对的，别把用户的活儿刷白），
但从前是一声不响地留着：源码改了、右边纹丝不动，真正的原因只有控制台看得见。
现在三处失败都在状态栏说一句「X 失败，右边仍是上一版谱面：…」，下一拍排出来了再自己收掉。

### 修复：右侧高亮的字与左侧选中的字偶发对不上

`_syncScoreToCursor` 从前拿 `selection.main.head` 反查图元，而选中一个词素时 `head` 落在**末尾**。
`.jpwabc` 里相邻词素之间常常没有空格（`2_1)`、`|1'.`、`|(6_`），末尾那一点正是下一个词素的起点
——于是点亮了**下一个**字，有没有空格决定它犯不犯，看着就是偶发。改为按选区**起点**反查。

### 变更：点音符只选中数字那一格

音符改挂两段区间：宽的（整个词素）管命中与光标联动——光标停在 `7,__` 的任何一格上都该点亮
这个音符；窄的（`PageItem.editSource`，只有数字那一格）管点选与改写——用户点的是那个数字，
改它不该连带把 `_` 减时线、`,` 八度逗号、弧线 `(` 一起换掉。
取哪一段只在 `editRangeOf`（`src/editor/inlineedit.ts`）一处判断。

### 变更：「按乐句重排 / 原始排版」跟着手改走

两个按钮现在都按**编辑器里当前这份文本**重排。从前 `.jpwabc` 那条读的是 `mixedXmlText`
（导入时的 MusicXML），手工补完延音线再点一下按钮，改动会被整篇覆盖且悄无声息。
在乐句档里手改一个字 → 按钮自己弹回「原始排版」，那份文本成为新的「原样」基准
（口径与文本谱那一侧的 `_setPuPhraseLayout` 对齐）。

代价：`.jpwabc` 那条多走一圈「文本 → Score」，`fromJpw` 装不下的东西（和弦、多声部）会掉
——但那些在编辑器文本里本来就已经没有了，右边谱面也一直是照这份文本排的。

回归 `scripts/phrase-edit-check.mjs`。

### 新增：演奏记号简写 `{yy}`

`{yy}` = `{YanYin}`（延音记号 fermata），另有 `{dy}` 顿音、`{by}` 波音、`{zy}` 重音。
大小写不分，可以并列（`{yy,zy}5`），**全名照样认**，老文件不受影响。
导出时写简写；要与原版 JP-Word 互通就把 `src/jpword/alias.ts` 的
`WRITE_SHORT_ARTICULATION` 设成 `false`。

实现是**交给词法器之前展开成全名**（`src/jpword/alias.ts`）——文法里那四个名字是写死的字面量，
而 `src/jpword/parser/` 是 ANTLR 生成的、改不动。展开会把那一行后面的列号挪后 4 格，
所以同时记一份位移账，三处消费端各自换算回原文位置：点选定位（`jpwimport.ts::srcOf`）、
语法着色（`tokens.ts::parseVoiceTokens`，那份 token 流是逐字符连续的契约）、
语法错误计数（`parse.ts::countVoiceSyntaxErrors`，不展开会把合法改动拦下）。

回归 `scripts/alias-check.mjs`。

### 文档

- 新增 `.jpwabc` 记谱语法速查（写进 [README](README.md#jpwabc-记谱语法速查)，
  `docs/记谱语法速查.md` 指向它）。**每一行都实测过**，并单列了「文法有、实际不能写」
  那一节——顺带查出两处既有缺口：`:|:` / `::` 直接写会让整篇解析失败；
  房号（1房/2房）三种写法全打不开，而 `jpscore.ts` 导出带房号的曲子时正好写的就是其中一种。
- `docs/架构与实现.md` 补「`.Voice` 里的花括号记号，与简写」一节（五种 `{…}` 的一览与现状）。
- `docs/实现/谱面就地编辑.md`、`docs/实现/乐句排版.md` 跟着这次的改动更新判据。

## 2026-09-14

### 新增：原样档的横版、只显示一行词、PNG 导出

设置面板给原样档加两项开关：**纸张横放**（A4/A5/B5/Letter 宽高对调，纸张选好后配合
「按乐句重排」即可让内容按新的纸宽重新断行）、**多段歌词叠排时只留编号最小的那一段**
（不用手动删改 `.Words` 源码）；导出菜单新增 **PNG（原样）**，按原样档当前设置直接出图，
与固定走展开档、逐段展开的 PPTX 导出互不影响。
