# `.jpwabc` 记谱语法速查

这一篇的正文搬进了 **[README 的「`.jpwabc` 记谱语法速查」一节](../README.md#jpwabc-记谱语法速查)**
——手改谱面时查那里，别在两处各记一份（会漂移）。

内容包含：音符本体（八度 / 升降 / 时值）、记号（弧线 / 花括号 / 小节线 / 换行 / 转调）、
演奏记号的简写、`.Voice` 之外的几段（`.Title` / `.Words` / `.Repeat` / `.Layout`）、
常见的手改场景，以及**文法有、实际不能写**（写了整篇打不开）那一节。

实现判据另见 [架构与实现](架构与实现.md) 的「`.Voice` 里的花括号记号，与简写」一节。
